spring:
  application:
    name: battcn-gateway
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        ext-config:
#          - refresh: true
#            data-id: app.properties
#            group: multi-data-ids
      discovery:
        server-addr: localhost:8848
    gateway:
      routes:
        - id: auth
          uri: lb://battcn-auth
          predicates:
            - Path=/**